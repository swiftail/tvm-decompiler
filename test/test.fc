#include "stdlib.fc";

() recv_internal(int in_msg_value, cell in_msg_full, slice in_msg) {
    throw_if(1, in_msg_value == 0);
}


() recv_external(cell in_msg_full, slice in_msg) {
in_msg.preload_uint()
}
